```cpp
#include <iostream>
using namespace std;

int main() {
	int size;
	cin >> size;

	for (int row = 1; row <= size; row++) {
		
		for (int columns = 1; columns <= size; columns++) {
			cout << "X";
			
		}
		cout << endl;
	}
}
```
